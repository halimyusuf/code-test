// import { render, screen } from "@testing-library/react";
// import App from "./App";

// throws error whenever i run test, tried to fix but collides with the backend configurations (babelrc)
test("renders learn react link", () => {
  // render(<App />);
  // const linkElement = screen.getByText(
  //   /Ask any question, use any resource you want/i
  // );
  // expect(linkElement).toBeInTheDocument();
});
